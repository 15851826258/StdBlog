@model IEnumerable<StdBlog.Models.m_recblog>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/m_Admin/_AdminLayout_rb.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pushtime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deletetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.blogid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pushtime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deletetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.blogid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
